{
  "name": "matrices",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "dev:react": "lerna run dev --scope @matrices/common --scope @matrices/react --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
